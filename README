= build =

To build these slides on Debian, you need these packages:
  texlive-latex-recommended latex-beamer texlive-latex-extra lmodern

To start the build, just use "make".

= about =
Homesite: http://www.FusionInventory.org
